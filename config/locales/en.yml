# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  blacklight:
    tools:
      save_workset: 'Create/Update Workset'

    application_name: 'HTRC Worksets'

    header_links:
      open_workset: 'Manage Worksets'

    registry:
       load:
          success: 'Workset "%{name}" opened'
       create:
          success: 'Workset "%{name}" created'
       update:
          success: 'Workset "%{name}" updated'
